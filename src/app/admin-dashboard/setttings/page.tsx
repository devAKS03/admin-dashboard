import AdminPage from "../../_components/admin-page";

export default function SettingsPage() {
  return (
    <AdminPage
      title="Settings"
      description="Configure platform-level preferences, admin profile options, and system controls inside a clean and professional white interface."
    />
  );
}
