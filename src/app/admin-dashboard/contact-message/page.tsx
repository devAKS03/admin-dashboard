import AdminPage from "../../_components/admin-page";

export default function ContactMessagePage() {
  return (
    <AdminPage
      title="Contact Message"
      description="Access incoming contact messages, assign ownership, and manage response workflows with a focused interface."
    />
  );
}
